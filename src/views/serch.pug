extends base.pug
include mixins/video 

block content
  form(method ="GET")
    input(placeholder="Title", required, type="text", name="keyword", maxlength=80)
    input(type="submit", value="Upload Video")
  div
    each video in videos 
      +video(video)
  