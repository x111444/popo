extends base


block content
    if errorMessage 
     li #{errorMessage}
    form(method="POST")
        input(name="username", type="text", required)
        input(name="password", type="password", required)
        input(type="submit", value="Log in")
    span did't you hava account?
    a(href="/join") go join 